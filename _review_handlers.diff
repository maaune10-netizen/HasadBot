diff --git a/hasad_bot/handlers/admin.py b/hasad_bot/handlers/admin.py
index 78c65a6..83f139b 100644
--- a/hasad_bot/handlers/admin.py
+++ b/hasad_bot/handlers/admin.py
@@ -714,51 +714,13 @@ async def admin_add_admin_done(update: Update, context):
     try:
         new_uid = int(update.message.text.strip())
 
-        # ✅ تحديث صلاحيات المستخدم
-        await db_set_user(new_uid, is_admin=1, joined_hijri=now_hijri())
-
-        # ✅ ========== إنشاء اشتراك عادي للأدمن الجديد ==========
-        from hasad_bot.database import create_user_subscription
-
-        u = await db_get_user(new_uid)
-        cur_exp = (u or {}).get("expiry_ts", 0) or 0
-
-        if cur_exp < time.time():
-            cur_exp = time.time()
-
-        # 30 يوماً للأدمن الجديد
-        end_date = cur_exp + (30 * 86400)
-
-        conn = await _db_pool.get_connection()
-        await conn.execute("""
-            UPDATE user_subscriptions SET is_active = 0
-            WHERE user_id = ? AND is_active = 1
-        """, (new_uid,))
-
-        await conn.execute("""
-            INSERT INTO user_subscriptions
-            (user_id, plan_id, start_date, end_date, max_homeworks, homeworks_used, is_active)
-            VALUES (?, 'monthly', ?, ?, 100, 0, 1)
-        """, (new_uid, cur_exp, end_date))
-        await conn.commit()
-        # ================================================================
-
-        await update.message.reply_text(
-            f"✅ تم ترقية <code>{new_uid}</code> إلى أدمن مع اشتراك شهري.",
-            parse_mode=ParseMode.HTML,
-            reply_markup=kb_admin()
+        from hasad_bot.admin_ops import add_admin
+        _, message = await add_admin(
+            new_uid,
+            actor_uid=update.effective_user.id,
+            actor_name=update.effective_user.full_name or "telegram",
         )
-
-        try:
-            await context.bot.send_message(
-                new_uid,
-                "👑 <b>تم تعيينك كأدمن في النظام!</b>\n\n"
-                "📦 تم تفعيل اشتراك شهري لك (100 واجب).\n"
-                "📅 يمكنك تجديد اشتراكك من لوحة التحكم.",
-                parse_mode=ParseMode.HTML
-            )
-        except:
-            pass
+        await update.message.reply_text(message, parse_mode=ParseMode.HTML, reply_markup=kb_admin())
 
     except Exception as e:
         await update.message.reply_text(f"❌ فشل: {e}")
diff --git a/hasad_bot/handlers/admin_reseller.py b/hasad_bot/handlers/admin_reseller.py
index e3d1052..60604ea 100644
--- a/hasad_bot/handlers/admin_reseller.py
+++ b/hasad_bot/handlers/admin_reseller.py
@@ -23,13 +23,9 @@ from hasad_bot.config import (
     AWAIT_RESELLER_PRICES,
 )
 from hasad_bot.database import (
-    is_bot_frozen, is_admin, is_reseller,
-    update_user_last_active, db_get_user, db_log,
-    get_reseller_credit, add_reseller_credit,
-    promote_to_reseller, promote_to_admin, demote_from_reseller, demote_from_admin,
-    get_reseller_stats, get_all_reseller_credit_prices,
-    set_reseller_credit_price, demote_from_reseller,
-    remove_customer_from_reseller,
+    is_admin, is_reseller,
+    update_user_last_active, db_get_user,
+    get_reseller_credit, get_all_reseller_credit_prices,
 )
 from hasad_bot.utils import admin_trace, now_hijri, kb_admin, config
 from hasad_bot.handlers.constants import (
@@ -149,59 +145,13 @@ async def admin_add_reseller_input(update: Update, context: ContextTypes.DEFAULT
         await update.message.reply_text("❌ أرسل معرف المستخدم (رقم صحيح).")
         return AWAIT_RESELLER_CREDIT_USER
 
-    # Check user exists
-    target_user = await db_get_user(target_id)
-    if not target_user:
-        await update.message.reply_text("❌ المستخدم غير موجود في البوت.")
-        return ADMIN_PANEL
-
-    # Check if already reseller or admin
-    if await is_reseller(target_id) or target_user.get('is_admin', 0) >= 1:
-        await update.message.reply_text("✅ هذا المستخدم موزع/أدمن بالفعل.")
-        return ADMIN_PANEL
-
-    is_owner = (uid == config.admin_id)
-
-    # Enforce MAX_FULL_ADMINS limit for owner promotions
-    if is_owner:
-        from hasad_bot.database import get_all_full_admins
-        current_admins = await get_all_full_admins()
-        if len(current_admins) >= config.max_full_admins:
-            await update.message.reply_text(
-                f"❌ تم الوصول للحد الأقصى من الأدمنز ({config.max_full_admins}).\n"
-                "احذف أدمن أولاً أو عدّل MAX_FULL_ADMINS في .env"
-            )
-            return ADMIN_PANEL
-
-    if is_owner:
-        ok = await promote_to_admin(target_id)
-        role_display = "أدمن (رئيس موردين)"
-    else:
-        ok = await promote_to_reseller(target_id)
-        role_display = "موزع"
-
-    if ok:
-        name = target_user.get('real_name') or target_user.get('name') or str(target_id)
-        await update.message.reply_text(
-            f"✅ <b>تمت الترقية بنجاح!</b>\n\n"
-            f"👤 المستخدم: {name}\n"
-            f"🆔 المعرف: {target_id}\n"
-            f"🏷️ الدور: {role_display}"
-        )
-        # Notify the new reseller/admin
-        try:
-            await context.bot.send_message(
-                chat_id=target_id,
-                text="🎉 <b>مبروك! لقد تمت ترقيتك!</b>\n\n"
-                     "🔑 يمكنك الآن الوصول إلى لوحة الإدارة من القائمة الرئيسية.\n"
-                     "💳 احصل على رصيد وابدأ في تفعيل الاشتراكات لعملائك!",
-                parse_mode="HTML"
-            )
-        except Exception:
-            pass
-        await db_log(uid, "PROMOTE_USER", detail=f"Promoted user {target_id} to {role_display}")
-    else:
-        await update.message.reply_text("❌ فشل في الترقية.")
+    from hasad_bot.admin_ops import add_reseller
+    _, message = await add_reseller(
+        target_id,
+        actor_uid=uid,
+        actor_name=update.effective_user.full_name or "telegram",
+    )
+    await update.message.reply_text(message, parse_mode="HTML")
 
     return ADMIN_PANEL
 
@@ -314,31 +264,15 @@ async def admin_reseller_credit_amount_input(update: Update, context: ContextTyp
         await update.message.reply_text("❌ خطأ في العملية.")
         return ADMIN_PANEL
 
-    ok = await add_reseller_credit(target_id, amount, details=f"Added by admin {uid}")
-    if ok:
-        new_balance = await get_reseller_credit(target_id)
-        target_user = await db_get_user(target_id)
-        name = target_user.get('real_name') or target_user.get('name') or str(target_id)
-        await update.message.reply_text(
-            f"✅ <b>تم الشحن بنجاح!</b>\n\n"
-            f"👤 الموزع: {name}\n"
-            f"💳 تم إضافة: {amount} credit\n"
-            f"💰 الرصيد الجديد: {new_balance} credit"
-        )
-        # Notify reseller
-        try:
-            await context.bot.send_message(
-                chat_id=target_id,
-                text=f"💰 <b>تم شحن رصيدك!</b>\n\n"
-                     f"💳 تم إضافة: {amount} credit\n"
-                     f"💰 رصيدك الحالي: {new_balance} credit",
-                parse_mode="HTML"
-            )
-        except Exception:
-            pass
-        await db_log(uid, "ADD_RESELLER_CREDIT", detail=f"Added {amount} to {target_id}")
+    actor_name = update.effective_user.full_name or "telegram"
+    action = context.user_data.get('admin_action', '')
+    if action == 'charge_admin_user':
+        from hasad_bot.admin_ops import charge_admin_credit
+        _, message = await charge_admin_credit(target_id, amount, actor_uid=uid, actor_name=actor_name)
     else:
-        await update.message.reply_text("❌ فشل في الشحن.")
+        from hasad_bot.admin_ops import add_reseller_credit_op
+        _, message = await add_reseller_credit_op(target_id, amount, actor_uid=uid, actor_name=actor_name)
+    await update.message.reply_text(message, parse_mode="HTML")
 
     return ADMIN_PANEL
 
@@ -353,15 +287,11 @@ async def admin_reseller_list(update: Update, context: ContextTypes.DEFAULT_TYPE
     if not await is_admin(uid):
         return MAIN_MENU
 
-    from hasad_bot.database.pool import db_pool
-
-    conn = await db_pool.get_connection()
-    cursor = await conn.execute(
-        """SELECT telegram_id, name, real_name, reseller_credit
-           FROM users WHERE role = 'reseller'
-           ORDER BY reseller_credit DESC"""
-    )
-    resellers = await cursor.fetchall()
+    from hasad_bot.admin_ops import list_resellers
+    success, resellers, message = await list_resellers(actor_uid=uid)
+    if not success:
+        await update.message.reply_text(message, parse_mode="HTML")
+        return ADMIN_PANEL
 
     if not resellers:
         text = """
@@ -374,9 +304,9 @@ async def admin_reseller_list(update: Update, context: ContextTypes.DEFAULT_TYPE
     else:
         is_owner = (uid == config.admin_id)
         lines = []
-        for i, (rid, name, real_name, credit) in enumerate(resellers, 1):
-            display = real_name or name or str(rid)
-            lines.append(f"{i}. {display} ({rid}) — 💳 {credit} credit")
+        for i, r in enumerate(resellers, 1):
+            display = r["name"] or str(r["uid"])
+            lines.append(f"{i}. {display} ({r['uid']}) — 💳 {r['credit']} credit")
 
         text = f"""
 📋 <b>قائمة الموزعين ({len(resellers)})</b>
@@ -459,16 +389,13 @@ async def admin_reseller_prices_input(update: Update, context: ContextTypes.DEFA
         await update.message.reply_text("❌ السعر يجب أن يكون أكبر من 0.")
         return AWAIT_RESELLER_PRICES
 
-    ok = await set_reseller_credit_price(plan_type, new_price)
-    if ok:
-        await update.message.reply_text(
-            f"✅ <b>تم تغيير السعر!</b>\n\n"
-            f"📦 الخطة: {plan_name_ar}\n"
-            f"💰 السعر الجديد: {new_price} credit"
-        )
-        await db_log(uid, "SET_RESELLER_PRICE", detail=f"{plan_type}: {new_price}")
-    else:
-        await update.message.reply_text("❌ فشل في تغيير السعر.")
+    from hasad_bot.admin_ops import set_reseller_prices_op
+    _, message = await set_reseller_prices_op(
+        {plan_type: new_price},
+        actor_uid=uid,
+        actor_name=update.effective_user.full_name or "telegram",
+    )
+    await update.message.reply_text(message, parse_mode="HTML")
 
     return ADMIN_PANEL
 
@@ -483,33 +410,17 @@ async def admin_reseller_stats_panel(update: Update, context: ContextTypes.DEFAU
     if not await is_admin(uid):
         return MAIN_MENU
 
-    from hasad_bot.database.pool import db_pool
-
-    conn = await db_pool.get_connection()
-
-    # Total resellers
-    cursor = await conn.execute("SELECT COUNT(*) FROM users WHERE role = 'reseller'")
-    total = (await cursor.fetchone())[0]
-
-    # Total credit distributed
-    cursor = await conn.execute(
-        "SELECT COALESCE(SUM(reseller_credit), 0) FROM users WHERE role = 'reseller'"
-    )
-    total_credit = (await cursor.fetchone())[0]
-
-    # Total keys generated
-    cursor = await conn.execute("SELECT COUNT(*) FROM reseller_keys")
-    total_keys = (await cursor.fetchone())[0]
-
-    # Total keys used
-    cursor = await conn.execute("SELECT COUNT(*) FROM reseller_keys WHERE used = 1")
-    total_used = (await cursor.fetchone())[0]
+    from hasad_bot.admin_ops import reseller_stats_op
+    success, stats, message = await reseller_stats_op(actor_uid=uid)
+    if not success:
+        await update.message.reply_text(message, parse_mode="HTML")
+        return ADMIN_PANEL
 
-    # Total customers via resellers
-    cursor = await conn.execute(
-        "SELECT COUNT(*) FROM users WHERE referred_by_reseller IS NOT NULL"
-    )
-    total_customers = (await cursor.fetchone())[0]
+    total = stats["total"]
+    total_credit = stats["total_credit"]
+    total_keys = stats["total_keys"]
+    total_used = stats["total_used"]
+    total_customers = stats["total_customers"]
 
     text = f"""
 📊 <b>إحصائيات الموزعين</b>
@@ -592,40 +503,13 @@ async def admin_handle_delete_reseller(update: Update, context: ContextTypes.DEF
         await update.message.reply_text("❌ أرسل معرف المستخدم (رقم صحيح).")
         return ADMIN_PANEL
 
-    if target_id == config.admin_id:
-        await update.message.reply_text("❌ لا يمكنك حذف نفسك!")
-        return ADMIN_PANEL
-
-    target_user = await db_get_user(target_id)
-    if not target_user:
-        await update.message.reply_text("❌ المستخدم غير موجود.")
-        return ADMIN_PANEL
-
-    if not await is_reseller(target_id):
-        await update.message.reply_text("❌ هذا المستخدم ليس موزعاً.")
-        return ADMIN_PANEL
-
-    # Demote
-    ok = await demote_from_reseller(target_id)
-    if ok:
-        name = target_user.get('real_name') or target_user.get('name') or str(target_id)
-        await update.message.reply_text(
-            f"✅ <b>تم حذف الموزع بنجاح!</b>\n\n"
-            f"👤 الموزع: {name}\n"
-            f"🆔 المعرف: {target_id}"
-        )
-        try:
-            await context.bot.send_message(
-                chat_id=target_id,
-                text="⚠️ <b>تم إزالة صفة الموزع من حسابك.</b>\n\n"
-                     "لم تعد تملك لوحة الموزع أو الرصيد.",
-                parse_mode="HTML"
-            )
-        except Exception:
-            pass
-        await db_log(uid, "DELETE_RESELLER", detail=f"Deleted reseller {target_id}")
-    else:
-        await update.message.reply_text("❌ فشل في الحذف.")
+    from hasad_bot.admin_ops import delete_reseller_op
+    _, message = await delete_reseller_op(
+        target_id,
+        actor_uid=uid,
+        actor_name=update.effective_user.full_name or "telegram",
+    )
+    await update.message.reply_text(message, parse_mode="HTML")
 
     return ADMIN_PANEL
 
@@ -669,40 +553,13 @@ async def admin_handle_delete_admin(update: Update, context: ContextTypes.DEFAUL
         await update.message.reply_text("❌ أرسل معرف المستخدم (رقم صحيح).")
         return ADMIN_PANEL
 
-    if target_id == config.admin_id:
-        await update.message.reply_text("❌ لا يمكنك حذف نفسك!")
-        return ADMIN_PANEL
-
-    target_user = await db_get_user(target_id)
-    if not target_user:
-        await update.message.reply_text("❌ المستخدم غير موجود.")
-        return ADMIN_PANEL
-
-    if target_user.get('is_admin', 0) < 1 and target_user.get('role') != 'admin':
-        await update.message.reply_text("❌ هذا المستخدم ليس أدمن.")
-        return ADMIN_PANEL
-
-    # Demote
-    ok = await demote_from_admin(target_id)
-    if ok:
-        name = target_user.get('real_name') or target_user.get('name') or str(target_id)
-        await update.message.reply_text(
-            f"✅ <b>تم حذف الأدمن بنجاح!</b>\n\n"
-            f"👤 الأدمن: {name}\n"
-            f"🆔 المعرف: {target_id}"
-        )
-        try:
-            await context.bot.send_message(
-                chat_id=target_id,
-                text="⚠️ <b>تم إزالة صفة الأدمن من حسابك.</b>\n\n"
-                     "لم تعد تملك لوحة الإدارة أو الصلاحيات الإدارية.",
-                parse_mode="HTML"
-            )
-        except Exception:
-            pass
-        await db_log(uid, "DELETE_ADMIN", detail=f"Deleted admin {target_id}")
-    else:
-        await update.message.reply_text("❌ فشل في الحذف.")
+    from hasad_bot.admin_ops import delete_admin
+    _, message = await delete_admin(
+        target_id,
+        actor_uid=uid,
+        actor_name=update.effective_user.full_name or "telegram",
+    )
+    await update.message.reply_text(message)
 
     return ADMIN_PANEL
 
@@ -784,81 +641,14 @@ async def admin_handle_ban_reseller_customer_callback(update: Update, context: C
         await q.edit_message_text("❌ خطأ في معرف المستخدم.")
         return
 
-    target_user = await db_get_user(target_id)
-    if not target_user:
-        await q.edit_message_text("❌ المستخدم غير موجود.")
-        return
-
-    customer_name = target_user.get('real_name') or target_user.get('name') or str(target_id)
-
-    if action == 'ban':
-        # Ban user: set free_attempts to 0 and deactivate subscription
-        from hasad_bot.database.pool import db_pool
-        conn = await db_pool.get_connection()
-
-        await conn.execute(
-            "UPDATE users SET free_attempts = 0, expiry_ts = 0 WHERE telegram_id = ?",
-            (target_id,)
-        )
-        await conn.execute(
-            "UPDATE user_subscriptions SET is_active = 0 WHERE user_id = ? AND is_active = 1",
-            (target_id,)
-        )
-        await conn.commit()
-
-        await q.edit_message_text(
-            f"🚫 <b>تم حظر المستخدم!</b>\n\n"
-            f"👤 العميل: {customer_name}\n"
-            f"✅ تم إيقاف الاشتراك وتصفير الرصيد"
-        )
-
-        # Notify customer
-        try:
-            await context.bot.send_message(
-                chat_id=target_id,
-                text="🚫 <b>تم إيقاف حسابك</b>\n\n"
-                     "لقد تم إلغاء اشتراكك وتصفير رصيدك.\n"
-                     "تواصل مع الإدارة لمعرفة السبب.",
-                parse_mode="HTML"
-            )
-        except Exception:
-            pass
-
-    elif action == 'stop':
-        # Stop subscription only
-        from hasad_bot.database.pool import db_pool
-        conn = await db_pool.get_connection()
-
-        await conn.execute(
-            "UPDATE user_subscriptions SET is_active = 0 WHERE user_id = ? AND is_active = 1",
-            (target_id,)
-        )
-        await conn.execute(
-            "UPDATE users SET expiry_ts = 0 WHERE telegram_id = ?",
-            (target_id,)
-        )
-        await conn.commit()
-
-        await q.edit_message_text(
-            f"⏹️ <b>تم إيقاف الاشتراك!</b>\n\n"
-            f"👤 العميل: {customer_name}\n"
-            f"✅ تم إلغاء الاشتراك النشط"
-        )
-
-        # Notify customer
-        try:
-            await context.bot.send_message(
-                chat_id=target_id,
-                text="⏹️ <b>تم إيقاف اشتراكك</b>\n\n"
-                     "لم يعد لديك اشتراك نشط.\n"
-                     "تواصل مع الإدارة لمعرفة التفاصيل.",
-                parse_mode="HTML"
-            )
-        except Exception:
-            pass
-
-    await db_log(uid, "BAN_RESELLER_CUSTOMER",
-                 detail=f"Action: {action}, Target: {target_id}")
+    from hasad_bot.admin_ops import ban_reseller_customer_op
+    _, message = await ban_reseller_customer_op(
+        target_id,
+        action,
+        actor_uid=uid,
+        actor_name=q.from_user.full_name or "telegram",
+    )
+    await q.edit_message_text(message)
 
 
 # ==============================================================================
@@ -872,9 +662,11 @@ async def admin_list_admins(update: Update, context: ContextTypes.DEFAULT_TYPE):
         await update.message.reply_text("❌ هذا الإجراء للمالك فقط.")
         return ADMIN_PANEL
 
-    from hasad_bot.database import get_all_full_admins
-
-    admins = await get_all_full_admins()
+    from hasad_bot.admin_ops import list_admins
+    success, admins, message = await list_admins(actor_uid=uid)
+    if not success:
+        await update.message.reply_text(message, parse_mode="HTML")
+        return ADMIN_PANEL
 
     if not admins:
         text = """
@@ -886,9 +678,9 @@ async def admin_list_admins(update: Update, context: ContextTypes.DEFAULT_TYPE):
 """
     else:
         lines = []
-        for i, (aid, name, tg_username, credit, created_at) in enumerate(admins, 1):
-            display = name or tg_username or str(aid)
-            lines.append(f"{i}. {display} ({aid}) — 💳 {credit} credit")
+        for i, r in enumerate(admins, 1):
+            display = r["name"] or r["tg_username"] or str(r["uid"])
+            lines.append(f"{i}. {display} ({r['uid']}) — 💳 {r['credit']} credit")
 
         text = f"""
 📋 <b>قائمة الأدمنز ({len(admins)})</b>
